server:
  port: 8888
spring:
  cloud:
    config:
      server:
        encrypt:
          enabled: false
        git:
          uri: https://github.com/cemreis0/spmia-config-repo/
          searchPaths: licensingservice, organizationservice